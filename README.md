# Python Backend Lab

Laboratorio de ingeniería backend con Python.

## Development

Create and activate the virtual environment, then install the project:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev]"
